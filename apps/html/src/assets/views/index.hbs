<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nx and Node Microservice</title>
</head>
<body>
    <ul class="todos">
        {{#each}}
            <li><input type="checkbox" {{#if done}}checked{{/if}} > {{message}}</li>
        {{/each}}
    </ul>
</body>
</html>